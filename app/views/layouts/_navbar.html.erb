		<div class="navbar">
			<p class="navbar-text pull-right">
			<% if user_signed_in? %>
			  Logged in as <strong><%= current_user.email %></strong>.
				<div class='navbar-links'>
					
					<%= link_to 'My Profile', show_my_profile_url, :class => 'navbar-link' %> |
				  <%= link_to 'Edit profile', edit_user_registration_url, :class => 'navbar-link' %> |
				  <%= link_to "Logout", destroy_user_session_url, method: :delete, :class => 'navbar-link'  %>
				</div>
		
			<% else %>
				<div class="navbar-links">
				  <%= link_to "Sign up", new_user_registration_url, :class => 'navbar-link'  %> |
				  <%= link_to "Login", new_user_session_url, :class => 'navbar-link'  %>
				</div>
				<% end %>
				
				<% if notice %>
				  <p class="alert alert-success"><%= notice %></p>
				<% end %>
				<% if alert %>
				  <p class="alert alert-danger"><%= alert %></p>
				<% end %>
			</p>
		</div>