<%= render 'layouts/alertbar' %>

<%= render 'layouts/navbar' %>

<div id="panel1" class="panel">
	<div class="bg-container">
		<div class="container">
			<%= render 'layouts/logo' %>
			<div class="sixteen columns" id="copy-container">
				<h1><%= yield :header %></h1>
				<h3><%= yield :sub_header1 %></h3>
				<h3><%= yield :sub_header2 %></h3>
			</div>
			<div class="spacer sixteen columns">
			</div>
			<div class="sixteen columns" id="cta-form">
				<%= render 'layouts/static_pages/browse_button' %>
				<span id="secure-notice">This is anonymous. We don't collect your name.</span>
			</div>
		</div>
	</div>
</div>