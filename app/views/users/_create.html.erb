	<%= form_for @user, url: { controller: "users", action: "new" } do |f| %>
	
		<%#= f.text_field :email, :type => "email", :placeholder => "john@example.com" %>
		<%#= f.text_field :zipcode, :placeholder => "Your zipcode" %> 
		<%= f.hidden_field :id %>
		<%= f.submit "Choose a Therapist", :class => "button browse-therapist-button" %>
	<% end %>